{
  "name": "lambda-function",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc && esbuild index.js --bundle --platform=node --target=node20 --outfile=dist/index.js",
    "test": "bun test",
    "lint": "eslint . --ext .ts",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "shared-utils": "file:../shared"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.129",
    "@types/node": "^22.0.0",
    "typescript": "^5.3.0",
    "esbuild": "^0.19.0",
    "jest": "^29.7.0"
  }
}
